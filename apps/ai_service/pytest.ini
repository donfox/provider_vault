[pytest]
markers =
    unit: Fast unit tests that use mocks (no external dependencies)
    integration: Integration tests that call real APIs (slow, costs money)
    slow: Tests that take a long time to run